#Auto generated by Edalize
MKFILE_DIR := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))

PDK_ROOT ?= {{pdk_root}}
PDK ?= {{pdk}}

.PHONY: all
all: librelane

.PHONY: librelane
librelane: $(PDK_ROOT)/$(PDK)
	$(EDALIZE_LAUNCHER) librelane librelane_config.yaml {{additional_config | join(' ')}}{% if additional_config %} {% endif%}--pdk ${PDK} --pdk-root ${PDK_ROOT} --manual-pdk --design-dir $(MKFILE_DIR)
